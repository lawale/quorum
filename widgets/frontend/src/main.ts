// Import all custom element components to register them.
// Each component uses <svelte:options customElement={{ tag: "..." }} />
// which auto-registers the custom element when the module is loaded.

import './components/ApprovalPanel.svelte';
import './components/RequestList.svelte';
import './components/StageProgress.svelte';
